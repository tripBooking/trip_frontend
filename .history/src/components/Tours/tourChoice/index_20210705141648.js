import React from 'react'
import styled from ''
export default function index(props) {
    const { data } = props
    console.log({data})
    return (
        <div>
            
        </div>
    )
}
