import React, { Component,useContext } from 'react'
import PropTypes from 'prop-types'

export default class ToursContainer extends Component {
    static propTypes = {
        prop: PropTypes
    }

    render() {
        const contextData = use
        return (
            <div>
                
            </div>
        )
    }
}
